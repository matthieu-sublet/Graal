name: quete_graal
description: Une application Flutter pour le livre interactif "Le Château des Ténèbres".
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.6.18
  provider: ^6.1.2
  google_fonts: ^6.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/livre.md
